/* Setup blank page controller */
angular.module('MetronicApp').controller('anularPT', ['$rootScope', '$scope', 'settings', '$http', '$sce', '$location',
        function($rootScope, $scope, settings, $http, $sce, $location) {

        }
    ]

);